language: node_js

services:
  - docker

node_js:
  - 6
  - 5
  - 4
notifications:
  email: false
branches:
  only:
    - master
before_install:
  - ./install-to-localhost.sh
script:
  - ./run-localhost-tests.sh
cache:
  directories:
    - node_modules
