language: node_js

services:
  - docker

sudo: required

os: 
  - linux
  - osx

node_js:
  - 6

notifications:
  email: false
  slack: mapic-io:NKaii0z3tRkeDyXaDbRauRFp


branches:
  only:
    - master

script:
  - ./install-to-localhost.sh travis

cache:
  directories:
    - $HOME/.yarn-cache
